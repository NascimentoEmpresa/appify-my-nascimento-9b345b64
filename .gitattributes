# Arquivos gerados automaticamente: colapsa o diff por padrao (no GitHub) e
# marca como sem-diff, sem tirar do versionamento. So esconde a VISUALIZACAO.
package-lock.json linguist-generated=true -diff
